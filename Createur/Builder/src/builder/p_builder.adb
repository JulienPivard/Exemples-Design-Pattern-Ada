package body P_Builder is

    ---------------------------------------------------------------------------
    procedure Ajouter_Titre
        (Monteur : in out T_Monteur; Texte : in Unbounded_Wide_Wide_String)
    is
        pragma Unreferenced (Monteur);
        pragma Unreferenced (Texte);
    begin
        null;
    end Ajouter_Titre;

    ---------------------------------------------------------------------------
    procedure Ajouter_Paragraphe
        (Monteur : in out T_Monteur; Texte : in Unbounded_Wide_Wide_String)
    is
        pragma Unreferenced (Monteur);
        pragma Unreferenced (Texte);
    begin
        null;
    end Ajouter_Paragraphe;

end P_Builder;
